type Query {
    dialogueFragments(projectId: String! language: String! queryString: String!): [Story]
}

type Story {
    _id: String
    type: DialogueFragmentType
    title: String
    storyGroupId: String
    projectId: String
}

enum DialogueFragmentType {
    story
    rule
}
